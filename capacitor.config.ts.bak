import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.controledespesas',
  appName: 'controledespesas',
  webDir: 'dist'
};

export default config;
